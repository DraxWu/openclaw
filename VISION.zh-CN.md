## OpenClaw 愿景

OpenClaw 是能真正动手做事的 AI。
它运行在你的设备上，使用你的渠道，遵循你的规则。

本文档说明项目的当前状态和发展方向。
我们仍处于早期阶段，因此迭代速度很快。
项目概览与开发文档：[`README.md`](README.md)
贡献指南：[`CONTRIBUTING.md`](CONTRIBUTING.md)

OpenClaw 始于一个学习 AI 并构建真正有用东西的个人试验场：
一个能在真实计算机上执行真实任务的助手。
它历经多个名称和形态的演变：Warelay → Clawdbot → Moltbot → OpenClaw。

目标：打造一个易用、支持广泛平台、尊重隐私与安全的个人助手。

**当前重点：**

**优先事项：**

- 安全与安全默认配置
- Bug 修复与稳定性
- 安装可靠性与首次运行用户体验

**下一阶段重点：**

- 支持所有主流模型提供商
- 改进主要消息渠道的支持（并增加若干高需求渠道）
- 性能与测试基础设施
- 更好的计算机使用和 Agent 执行能力
- CLI 与 Web 前端的易用性
- 在 macOS、iOS、Android、Windows、Linux 上的配套应用

**贡献规则：**

- 一个 PR = 一个 issue/主题。不要打包多个不相关的修复/功能。
- 变更超过约 5,000 行的 PR 仅在特殊情况下才会审查。
- 不要一次提交大量小型 PR；每个 PR 都有审查成本。
- 对于非常小的相关修复，鼓励合并成一个聚焦的 PR。

## 安全

OpenClaw 的安全是有意为之的权衡：强默认配置，同时不牺牲能力。
目标是保持对实际工作的强大支持，同时让高风险路径变得显式且由操作者控制。

权威安全策略与漏洞报告：

- [`SECURITY.md`](SECURITY.md)

我们优先保证安全默认，但也为受信任的高权限工作流提供清晰的开关。

## 插件与记忆

OpenClaw 拥有丰富的插件 API。
核心保持精简；可选能力通常应以插件形式交付。

首选的插件路径是通过 npm 包分发，开发时通过本地扩展加载。
如果你构建插件，应在自己的仓库中托管和维护。
将可选插件加入核心的门槛刻意设得很高。
插件文档：[`docs/tools/plugin.md`](docs/tools/plugin.md)
社区插件列表与 PR 门槛：https://docs.openclaw.ai/plugins/community

记忆是一种特殊的插件槽位，同一时间只能有一个记忆插件处于激活状态。
目前我们提供多种记忆选项；长期计划是收敛到一条推荐的默认路径。

### Skills（技能）

我们仍随核心打包一些技能以提供基础用户体验。
新技能应首先发布到 ClawHub（`clawhub.ai`），而非默认加入核心。
核心技能的新增应罕见，且需要强有力的产品或安全理由。

### MCP 支持

OpenClaw 通过 `mcporter` 支持 MCP：https://github.com/steipete/mcporter

这样使 MCP 集成保持灵活并与核心运行时解耦：

- 添加或更改 MCP 服务器时无需重启 gateway
- 保持核心工具/上下文表面精简
- 降低 MCP 变更对核心稳定性和安全性的影响

目前我们更青睐这种桥接模式，而非在核心中构建一流的 MCP 运行时。
如果 `mcporter` 尚未支持某个 MCP 服务器或功能，请在那里提交 issue。

### Setup（安装）

OpenClaw 目前在设计上以终端为首选。
这使安装过程保持显式：用户能 upfront 看到文档、认证、权限和安全态势。

长期而言，随着安全加固的成熟，我们希望有更轻松的上手流程。
我们不希望用便捷封装把关键安全决策对用户隐藏。

### 为什么选择 TypeScript？

OpenClaw 本质上是一个编排系统：提示词、工具、协议与集成。
选择 TypeScript 是为了让 OpenClaw 天然可 hack。
它广为人知，迭代迅速，易于阅读、修改和扩展。

## 我们暂时不会合并的内容

- 可以放在 ClawHub 上的新核心技能
- 所有文档的完整翻译集（延后；我们计划后续用 AI 生成翻译）
- 不明确属于模型提供商类别的商业服务集成
- 围绕已支持渠道的包装渠道，且无明确能力或安全缺口
- 在 `mcporter` 已提供集成路径的情况下，将一流 MCP 运行时纳入核心
- Agent 层级框架（管理者套管理者 / 嵌套规划树）作为默认架构
- 与现有 agent 和工具基础设施重复的沉重编排层

此列表是路线图的护栏，而非物理定律。
强烈的用户需求和充分的技术理由可以改变它。
